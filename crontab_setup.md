# Настройка автоматической перезагрузки сервера через crontab

## Вариант 1: Прямая команда в crontab

Откройте crontab для редактирования:
```bash
crontab -e
```

Добавьте одну из следующих строк (выберите нужное расписание):

### Перезагрузка каждый день в 3:00 ночи
```
0 3 * * * /sbin/reboot
```

### Перезагрузка каждый день в 4:00 утра
```
0 4 * * * /sbin/reboot
```

### Перезагрузка каждую неделю в воскресенье в 3:00
```
0 3 * * 0 /sbin/reboot
```

### Перезагрузка каждые 7 дней в 3:00
```
0 3 */7 * * /sbin/reboot
```

### Перезагрузка каждую неделю в понедельник в 2:00
```
0 2 * * 1 /sbin/reboot
```

## Вариант 2: Использование скрипта (рекомендуется)

1. Сделайте скрипт исполняемым:
```bash
chmod +x reboot_server.sh
```

2. Переместите скрипт в системную директорию (опционально):
```bash
sudo mv reboot_server.sh /usr/local/bin/
```

3. Добавьте в crontab:
```bash
crontab -e
```

4. Добавьте строку (пример - каждый день в 3:00):
```
0 3 * * * /path/to/reboot_server.sh
```

Или если скрипт в /usr/local/bin:
```
0 3 * * * /usr/local/bin/reboot_server.sh
```

## Формат crontab

```
минута час день месяц день_недели команда
```

Примеры:
- `0 3 * * *` - каждый день в 3:00
- `0 */6 * * *` - каждые 6 часов
- `0 0 * * 0` - каждое воскресенье в полночь
- `0 3 1 * *` - 1-го числа каждого месяца в 3:00

## Проверка crontab

Просмотр текущих задач:
```bash
crontab -l
```

Проверка логов cron:
```bash
# Для системного cron
sudo tail -f /var/log/syslog | grep CRON

# Для пользовательского cron (зависит от системы)
grep CRON /var/log/syslog
```

## Важно

- Для перезагрузки через crontab обычно нужны права root
- Если используете пользовательский crontab, может потребоваться настроить sudo без пароля для команды reboot
- Рекомендуется использовать системный crontab (sudo crontab -e) для задач перезагрузки

## Настройка sudo без пароля для reboot (если нужно)

Отредактируйте sudoers:
```bash
sudo visudo
```

Добавьте строку (замените username на ваше имя пользователя):
```
username ALL=(ALL) NOPASSWD: /sbin/reboot
```

